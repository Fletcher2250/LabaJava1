# Тарасов Елисей ИТ-16 Лабораторная работа №1
# Задание 1
## Задача 1
## Текст задачи
Дробная часть.
Дана сигнатура метода: public double fraction (double x);
Необходимо реализовать метод таким образом, чтобы он возвращал только
дробную часть числа х. Подсказка: вещественное число может быть
преобразовано к целому путем отбрасывания дробной части.
Пример:
x=5,25
результат: 0,25
## Алгоритм решения
1. (int) x — превращает 5.25 в 5 (просто откидывает всё после запятой).
2. x - (int) x → 5.25 - 5 → 0.25 — это и есть дробная часть.
## Задача 2
## Текст задачи
Сумма знаков.
Дана сигнатура метода: public int sumLastNums (int x);
Необходимо реализовать метод таким образом, чтобы он возвращал результат
сложения двух последних знаков числах, предполагая, что знаков в числе не
менее двух. Подсказки:
int x=123%10; // х будет иметь значение 3
int у=123/10; // у будет иметь значение 12
Пример:
x=4568
результат: 14
## Алгоритм решения
1.	Math.abs(x) гарантирует, что число положительное.
2.	Оператор % 10 всегда выделяет последнюю цифру числа.
3.	Оператор / 10 отрезает последнюю цифру числа.
4.	Комбинируя их: (x / 10) % 10 сначала отрезает последнюю цифру, а затем % 10 берет новую последнюю.
5.	В конце мы просто складываем эти две найденные цифры.
## Задача 3
## Текст задачи
Большая буква.
Дана сигнатура метода: public bool isUpperCase (char x);
Необходимо реализовать метод таким образом, чтобы он принимал символ x и
возвращал true, если это большая буква в диапазоне от ‘A’ до ‘Z’.
Пример 1:
x=’D’
результат: true
Пример 2:
x=’q’
результат: false
## Алгоритм решения
1.	В Java каждый символ (char) имеет свой числовой код.
2.	Коды больших латинских букв идут подряд: от 'A' до 'Z'.
3.	Условие x >= 'A' && x <= 'Z' проверяет, попадает ли код нашего символа в этот диапазон. Если да — возвращает true, если нет — возвращает false
## Задача 4
## Текст задачи
Диапазон.
Дана сигнатура метода: public bool isInRange (int a, int b, int num);
Метод принимает левую и правую границу (a и b) некоторого числового
диапазона. Необходимо реализовать метод таким образом, чтобы он возвращал
true, если num входит в указанный диапазон (включая границы). Обратите
внимание, что отношение a и b заранее неизвестно (неясно кто из них больше, а
кто меньше)
Пример 1:
a=5 b=1 num=3
результат: true
Пример 2:
a=2 b=15 num=33
результат: false
## Алгоритм решения
1.	Так как заранее неизвестно, какое из чисел (a или b) больше, мы сначала находим минимальную и максимальную границу с помощью Math.min и Math.max.
2.	Затем просто проверяем: попадает ли num в этот диапазон — то есть num >= min И num <= max.
## Задача 5
## Текст задачи
Многократный вызов.
Дана сигнатура метода: public int lastNumSum(int a, int b)
Необходимо реализовать метод таким образом, чтобы он считал сумму цифр
двух чисел из разряда единиц. Выполните с его помощью последовательное
сложение пяти чисел и результат выведите на экран. Постарайтесь выполнить
задачу, используя минимально возможное количество вспомогательных
переменных.
Пример:
5+11 это 6
6+123 это 9
9+14 это 13
13+1 это 4
Итого 4
## Алгоритм решения
Метод lastNumSum(a, b) берёт последнюю цифру от каждого числа (a % 10 и b % 10) и складывает их.
# Задание 2
## Задача 1
## Текст задачи
Модуль числа.
Дана сигнатура метода: public int abs (int x);
Необходимо реализовать метод таким образом, чтобы он возвращал модуль
числа х (если оно было положительным, то таким и остается, если он было
отрицательным – то необходимо вернуть его без знака минус).
Пример 1:
x=5
результат: 5
Пример 2:
x=-3
результат: 3
## Алгоритм решения
1.	Если число меньше 0 → вернуть -x (убрать минус).
2.	Иначе → вернуть x как есть
## Задача 2
## Текст задачи
Строка сравнения.
Дана сигнатура метода: public String makeDecision (int x, int y);
Необходимо реализовать метод таким образом, чтобы он возвращал строку,
которая включает два принятых методом числа и корректно выставленный
знак операции сравнения (больше, меньше, или равно).
Пример 1:
x=5 y=7
результат: “5< 7”
Пример 2:
x=8 y=-1
результат: “8 >-1”
Пример 3:
x=4 y=4
результат: “4==4”
## Алгоритм решения
1.	Если x < y → вернуть строку "x < y".
2.	Если x > y → вернуть строку "x > y".
3.	Иначе (равны) → вернуть строку "x == y".
## Задача 3
## Текст задачи
Тройной максимум.
Дана сигнатура метода: public int max3 (int x, int y, int z);
Необходимо реализовать метод таким образом, чтобы он возвращал
максимальное из трех полученных методом чисел. Подсказка: идеальное
решение включает всего две инструкции if и не содержит вложенных if.
Пример 1:
x=5 y=7 z=7
результат: 7
Пример 2:
x=8 y=-1 z=4
результат: 8
## Алгоритм решения
1.	Предположим, что максимум — это x.
2.	Если y больше текущего максимума → обновим максимум.
3.	Если z больше текущего максимума → обновим максимум.
4.	Вернём найденный максимум.
## Задача 4
## Текст задачи
Возраст.
Дана сигнатура метода: public String age (int x);
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в
которой сначала будет число х, а затем одно из слов:
год
года
лет
Слово “год” добавляется, если число х заканчивается на 1, кроме числа 11.
Слово “года” добавляется, если число х заканчивается на 2, 3 или 4, кроме чисел
12, 13, 14.
Слово “лет”добавляется во всех остальных случаях.
Подсказка: оператор % позволяет получить остаток от деления.
Пример 1:
x=5
результат: “5 лет”
Пример 2:
x=31
результат: “31 год”
Пример 3:
x=44
результат: “44 года”
## Алгоритм решения
1.	Получить последнюю цифру: x % 10
2.	Получить последние две цифры: x % 100
3.	Если последние две цифры от 11 до 14 → "лет" (исключение)
4.	Иначе если последняя цифра 1 → "год"
5.	Иначе если последняя цифра 2, 3, 4 → "года"
6.	Иначе → "лет"
## Задача 5
## Текст задачи
День недели.
Дана сигнатура метода: public String day (int x);
Метод принимает число x, обозначающее день недели. Необходимо реализовать
метод таким образом, чтобы он возвращал строку, которая будет обозначать
текущий день недели, где 1- это понедельник, а 7 – воскресенье. Если число не
от 1 до 7 то верните текст “это не день недели”. Вместо if в данной задаче
используйте switch.
Пример:
x=5
результат: “пятница”
## Алгоритм решения
Используем switch по числу x:
1. 1 → "понедельник", 2 → "вторник", ..., 7 → "воскресенье"
2.	default → "это не день недели"
# Задание 3
## Задача 1
## Текст задачи
Числа наоборот.
Дана сигнатура метода: public String reverseListNums (int x);
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в
которой будут записаны все числа от x до 0 (включительно).
Пример:
x=5
результат: “5 4 3 2 1 0”
## Алгоритм решения
1.	Проверка входных данных: если x < 0 — вернуть сообщение об ошибке.
2.	Сборка строки: использовать StringBuilder.
3.	Цикл: от x до 0 включительно, на каждой итерации добавлять число и пробел.
4.	Убрать лишний пробел в конце.
5.	Возврат: преобразовать StringBuilder в String через toString().
## Задача 2
## Текст задачи
Четные числа.
Дана сигнатура метода: public String chet (int x);
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в
которой будут записаны все четные числа от 0 до x (включительно). Подсказа
для обеспечения качества кода: инструкцию if использовать не следует.
Пример:
x=9
результат: “0 2 4 6 8”
## Алгоритм решения
1.	Проверка данных: если x < 0 — вернуть сообщение об ошибке.
2.	Сборка строки: использовать StringBuilder, сразу добавив в него "0".
3.	Цикл без if: идти от 2 до x с шагом 2 — так все числа автоматически будут чётными, и проверка на чётность не нужна.
4.	Добавление пробела: перед каждым новым числом добавлять пробел.
5.	Возврат: преобразовать StringBuilder в String
## Задача 3
## Текст задачи
Квадрат.
Дана сигнатура метода: public void square (int x);
Необходимо реализовать метод таким образом, чтобы он выводил на экран
квадрат из символов ‘*’ размером х, у которого х символов в ряд и х символов в
высоту.
Пример 1:
x=2
результат:
**
**
Пример 2:
x=4
результат:
****
****
****
****
## Алгоритм решения
1. Проверка данных: если x <= 0 — ничего не выводим.
2.	Внешний цикл: по строкам квадрата — от 0 до x-1.
3.	Внутренний цикл: по символам в строке — от 0 до x-1.
4.	Перенос строки: после внутреннего цикла выводим System.out.println() для перехода на новую строку.
## Задача 4
## Текст задачи
Левый треугольник.
Дана сигнатура метода: public void leftTriangle (int x);
Необходимо реализовать метод таким образом, чтобы он выводил на экран
треугольник из символов ‘*’ у которого х символов в высоту, а количество
символов в ряду совпадает с номером строки.
Пример 1:
x=2
результат:
*
**
Пример 2:
x=4
результат:
*
**
***
****
## Алгоритм решения
1.	Проверка данных: если x <= 0 — вывести сообщение об ошибке.
2.	Внешний цикл: по строкам треугольника — от 1 до x.
3.	Внутренний цикл: по символам в строке — от 0 до i-1.
4.	Перенос строки: после внутреннего цикла выводим System.out.println().
## Задача 5
## Текст задачи
Угадайка.
Дана сигнатура метода: public void guessGame()
Необходимо реализовать метод таким образом, чтобы он генерировал
случайное число от 0 до 9, далее считывал с консоли введенное пользователем
число и выводил, угадал ли пользователь то, что было загадано, или нет. Метод
запускается до тех пор, пока пользователь не угадает число. После этого
выведите на экран количество попыток, которое потребовалось пользователю,
чтобы угадать число.
Пример:
Введите число от 0 до 9:
5
Вы не угадали, введите число от 0 до 9:
9
Вы угадали!
Вы отгадали число за 2 попытки
## Алгоритм решения
1.	Создать Scanner для чтения ввода с консоли.
2.	Сгенерировать случайное число от 0 до 9 через Math.random() * 10.
3.	Инициализировать счётчик попыток attempts = 0.
4.	Цикл while (true) — работает, пока пользователь не угадает:
o	Вывести приглашение "Введите число от 0 до 9:".
o	Проверить ввод через hasNextInt() — если не число, очистить буфер и повторить.
o	Считать число и проверить, что оно в диапазоне 0–9.
o	Увеличить счётчик попыток.
o	Если число совпало — вывести "Вы угадали!" и количество попыток, break.
o	Иначе — "Вы не угадали, введите число от 0 до 9:".
5.	Закрыть Scanner после завершения.
# Задание 4
## Задача 1
## Текст задачи
Поиск максимального.
Дана сигнатура метода: public int maxAbs (int[] arr);
Необходимо реализовать метод таким образом, чтобы он возвращал
наибольшее по модулю (то есть без учета знака) значение массива arr.
Пример:
arr=[1,-2,-7,4,2,2,5]
результат: -7
## Алгоритм решения
1.	Проверка данных: если массив null или пустой — вернуть 0.
2.	Инициализация: сохранить модуль первого элемента как текущий максимум, а сам первый элемент — как результат.
3.	Цикл по массиву: для каждого элемента сравнивать его модуль с текущим максимумом.
4.	Обновление: если модуль текущего элемента больше — обновляем и максимум, и результат.
5.	Возврат: вернуть сам элемент, а не его модуль.
## Задача 2
## Текст задачи
Добавление в массив.
Дана сигнатура метода: public int[]add (int[] arr, int x, int pos);
Необходимо реализовать метод таким образом, чтобы он возвращал новый
массив, который будет содержать все элементы массива arr, однако в позицию
pos будет вставлено значение x.
Пример:
arr=[1,2,3,4,5]
x=9
pos=3
результат: [1,2,3,9,4,5]
## Алгоритм решения
1.	Проверка данных: если pos вне диапазона [0, arr.length] — вернуть исходный массив.
2.	Создать новый массив размером arr.length + 1.
3.	Копирование в три этапа:
o	элементы с индексами [0, pos) — без изменений.
o	на позицию pos записать x.
o	элементы с индексами [pos, arr.length) — со сдвигом на 1 вправо.
4.	Вернуть новый массив.
## Задача 3
## Текст задачи
Добавление массива в массив.
Дана сигнатура метода: public int[] add (int[] arr, int[] ins, int pos);
Необходимо реализовать метод таким образом, чтобы он возвращал новый
массив, который будет содержать все элементы массива arr, однако в позицию
pos будут вставлены значения массива ins.
Пример:
arr=[1,2,3,4,5]
ins=[7,8,9]
pos=3
результат: [1,2,3,7,8,9,4,5]
## Алгоритм решения
1.	Проверка данных: если pos вне диапазона [0, arr.length] — вернуть исходный массив.
2.	Создать новый массив размером arr.length + ins.length.
3.	Копирование в три этапа:
o	элементы arr[0..pos) — без изменений.
o	элементы ins[0..ins.length) — вставка на позицию pos.
o	элементы arr[pos..arr.length) — со сдвигом на ins.length.
4.	Вернуть новый массив.
## Задача 4
## Текст задачи
Реверс.
Дана сигнатура метода: public void reverse (int[] arr);
Необходимо реализовать метод таким образом, чтобы он изменял массив arr.
После проведенных изменений массив должен быть записан задом-наперед.
Пример:
arr=[1,2,3,4,5]
результат: arr=[5,4,3,2,1]
## Алгоритм решения
1.	Проверка данных: если массив null или его длина меньше 2 — ничего не делаем.
2.	Два указателя: left = 0 и right = arr.length - 1.
3.	Цикл while (left < right): меняем местами элементы arr[left] и arr[right] через временную переменную, затем сдвигаем left++ и right--.
4.	Завершение: когда указатели встретятся, массив развёрнут. Метод void — меняет массив на месте, ничего не возвращает.
## Задача 5
## Текст задачи
Все вхождения.
Дана сигнатура метода: public int[] findAll (int[] arr, int x);
Необходимо реализовать метод таким образом, чтобы он возвращал новый
массив, в котором записаны индексы всех вхождений числа x в массив arr.
Пример:
arr=[1,2,3,8,2,2,9]
x=2
результат: [1,4,5]
## Алгоритм решения
1.	Проверка данных: если массив null — вернуть пустой массив.
2.	Создать ArrayList<Integer> для динамического сбора индексов.
3.	Один проход по массиву: если arr[i] == x — добавляем индекс i в список.
4.	Преобразовать ArrayList в int[] — создать массив нужного размера и скопировать элементы.
5.	Вернуть полученный массив.














